#! /bin/bash

log=Saturday

# 双引号会对其中的"$"字符进行解释
# 双引号会阻止Shell对大多数特殊字符(例如#)进行解释，但对美元符号
# 倒引号，和双引号仍然保持其特殊函数，倒引号通常位于ESC下方
echo "Today is $log"

# 单引号不会对特殊字符进行解释
# 单引号会组织Shell对所有字符进行解释
echo 'Today is $log'

# 倒引号会运行其中的命令，并把命令输出作为最终结果
# 倒引号括起一个Shell命令时，这个命令将会被执行，执行后的输出结果
# 将作为这个表达式的值
echo "Today is `date`"
